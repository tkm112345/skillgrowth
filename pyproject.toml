[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.ruff.lint.per-file-ignores]
# LLM prompt templates contain long embedded JSON/example lines; rewrapping
# them risks changing what's actually sent to the model.
"app/llm.py" = ["E501"]
